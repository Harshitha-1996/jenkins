pipeline{
    agentt any
    options{
        timeout(time: 10 , unit: SECONDS)
        timestaps()
        retry(1)
    }
    stages{
        stage("checkout"){
            steps{
                echo "hello"
                sh "sleep 60"
            }

        }
    }
}